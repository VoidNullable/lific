# Keep the build context to what the image actually needs.
.git
target
web/node_modules
web/dist
site
promo
docs
backups
*.db
*.db-wal
*.db-shm
AGENTS.md
deploy.sh
# Credentials and local-only state must never enter a build context.
*.env
attachments
e2e
.dev
.openchamber
*.kra
*~
